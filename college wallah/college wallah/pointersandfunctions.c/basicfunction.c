#include<stdio.h>
#include<math.h>
int main()
{   int a;
    printf("a=");
    scanf("%d",&a);
    // int root=sqrt(a);
    // printf("square root=%d",root);
    int power=pow(a,5);
    printf("the power =%d",power);
    return 0;
}